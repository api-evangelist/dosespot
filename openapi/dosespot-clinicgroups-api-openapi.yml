openapi: 3.2.0
info:
  version: Full_EPCSV2
  title: 'DoseSpot REST API - Plan: Full + EPCS - V2 Clinic Groups API'
servers:
- url: https://virtserver.swaggerhub.com/DoseSpot/dosespot-staging-rest-api-v2/Full_EPCSV2
tags:
- name: ClinicGroups
paths:
  /api/clinicGroups:
    post:
      tags:
      - ClinicGroups
      summary: Manages clinic groups
      description: Handles clinic group actions which include adding and modifying clinic list in a clinic group.
      operationId: ClinicGroups_AddEditClinicGroupV2
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AddEditClinicGroupResponse'
            text/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AddEditClinicGroupResponse'
            application/problem+json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AddEditClinicGroupResponse'
            application/xml:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AddEditClinicGroupResponse'
            text/xml:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AddEditClinicGroupResponse'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddEditClinicGroupRequest'
          text/json:
            schema:
              $ref: '#/components/schemas/AddEditClinicGroupRequest'
          application/problem+json:
            schema:
              $ref: '#/components/schemas/AddEditClinicGroupRequest'
          application/xml:
            schema:
              $ref: '#/components/schemas/AddEditClinicGroupRequest'
          text/xml:
            schema:
              $ref: '#/components/schemas/AddEditClinicGroupRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/AddEditClinicGroupRequest'
        required: true
components:
  schemas:
    AddEditClinicGroupRequest:
      type: object
      required:
      - ClinicIds
      properties:
        ClinicIds:
          type: array
          items:
            type: integer
            format: int32
        ClinicGroupName:
          type: string
    AddEditClinicGroupResponse:
      type: object
      properties:
        UserId:
          type: integer
          format: int32
        ClientId:
          type: integer
          format: int32
        ClientName:
          type: string
        ClinicIds:
          type: array
          items:
            type: integer
            format: int32
        ClinicGroupName:
          type: string
        Result:
          description: The result of the operation.
          $ref: '#/components/schemas/Result'
    Result:
      type: object
      required:
      - ResultCode
      properties:
        ResultCode:
          type: string
          description: The result code of the operation.
        ResultDescription:
          type: string
          description: A description of the result, if there were errors.
      description: Represents the result of a service operation.
